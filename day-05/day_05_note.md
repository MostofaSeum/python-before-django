1. What is file handling?
Ans: File handling in Python refers to the process of creating, opening, reading, writing, and closing files directly through code

2. Difference between read, write, and append mode.
Ans: Read mode reads the whole file, Write mode Overwrites the whole file, Append mode add new written text at the end of the file.

3. Why do we use with open()?
Ans: To open a file. After using it we can read or write a file.

4. What is exception handling?
Ans: Writing code in a way that handles any kind of unexpected situations and it doesn't stop the running of the program.

5. Why do we use try-except?
Ans: We use it to prevent any crash and handle if any exception occurs

6. What is pip?
Ans: Pip is a package manager for Python 

7. What is a virtual environment?
Ans: A virtual environment is a isolated environment for Python projects

8. What is requirements.txt?
Ans: A text file where all the informations about installed packages are available. And another user can easily download packages using it.

9. Which commands did you use today?
Ans: pip install package-name, pip freeze > requirements.txt
10. What problems did you face during practice?
Ans: 
11. What did you learn today?